# 2026-07-30 OHB 鸿蒙 Bilibili 客户端

## 完成：P0 V1 达标任务（Task 1~10 全部）
- 从 Hello World 模板起步，实现完整 V1：分层架构(common/data/network/player/models/pages/viewmodel/services/theme) + Navigation+Tabs 主框架
- 网络层：Result<T>/DataState<T>/AppError（AP-2，非三态）、WBI 签名（mixinKeyEncTab+wts+w_rid，MD5 走 cryptoFramework）、AppSign（TV 登录）、CookieManager 按账户隔离持久化、HttpClient（重试/拦截/包络解析/nav 自动刷 WBI key）
- 认证：AccountManager 多账户 + TV 扫码登录（auth_code→poll 86039/86090/86038/0）+ Asset Store 加密存凭证 + 按 mid 的 Cookie/设置命名空间
- 播放器：IPlayerEngine→AvPlayerEngine（AVPlayer 状态机）+PlayerController（@Observed 状态流、全屏/亮度/音量/seek）；XComponent SURFACE 渲染
- 页面：首页推荐流(下拉刷新/分页/过滤/响应式列数)、搜索(建议/热搜/历史/五类Tab/排序时长分区筛选)、动态流(视频/图文卡片)、我的(多账户切换)、登录(二维码+倒计时+过期)、视频详情(播放/分P/简介)
- 编译：hvigorw assembleHap 0 报错，产出 entry-default-signed.hap

## 关键决策（文档歧义，已向用户报备）
1. TV 登录无独立 confirm 步骤（PiliPlus 实际也是 poll 成功即给 token，tasks.md 写的 qrcodeConfirm 未使用）
2. 播放地址用 fnval=1 muxed MP4（spec 参考的 fnval=4048 DASH 音画分离，AVPlayer V1 播不了；DASH 留给 P1 Task 12）
3. 凭证用 Asset Store 而非 HUKS（同级安全能力，更适合键值凭证）
4. local.properties sdk.dir 原指向不存在的 Sdk14，已改为 ~/Library/OpenHarmony/Sdk

## 待办/遗留
- 真机/模拟器端到端运行验证未做（无设备）：TR-10.2/10.3 待用户在 DevEco 中运行验证
- P1：Task 11 设置导入导出、Task 12 弹幕+清晰度+倍速+PiP
